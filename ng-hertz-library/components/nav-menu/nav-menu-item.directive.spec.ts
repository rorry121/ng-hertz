import { HzNavMenuItemDirective } from './nav-menu-item.directive';

describe('HzNavMenuItemDirective', () => {
  it('should create an instance', () => {
    const directive = new HzNavMenuItemDirective();
    expect(directive).toBeTruthy();
  });
});
